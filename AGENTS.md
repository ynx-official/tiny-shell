# AGENTS.md

本文件适用于仓库根目录及其全部子目录。

## 项目原则

- 修改应聚焦用户需求，优先修复根因，不做无关重构。
- 保持现有 Rust、GPUI、异步任务和配置存储模式，不引入平行架构。
- 不覆盖、回退或整理用户尚未提交的改动。
- 新功能必须同时考虑 Windows、macOS 和 Linux；平台专属逻辑必须使用 `cfg` 明确隔离。

## UI 组件规范

- UI 功能必须优先使用 `gpui-component` 已提供的组件和扩展。
- 右键菜单使用 `ContextMenuExt`、`PopupMenu` 和 `PopupMenuItem`。
- 带子菜单的菜单使用 `PopupMenu::submenu`，禁止自行实现全屏遮罩、绝对定位子菜单和菜单焦点状态机。
- 下拉菜单使用 `DropdownMenu`；对话框使用现有 `WindowExt`、`Dialog` 等组件。
- 只有在 `gpui-component` 确实没有对应能力时，才允许实现自定义组件；实现前必须搜索依赖源码和项目现有用法，并在代码评审说明缺失能力与选择理由。
- 禁止在一次鼠标事件中同步重建可点击叠层，避免 Windows 下的点击穿透、事件重入和焦点 panic。
- 菜单、弹窗和浮层必须考虑窗口边缘定位、焦点恢复、点击外部关闭和事件传播。

## Rust 代码规范

- 遵循现有模块边界；业务操作放在对应模块，渲染代码只负责组装 UI 和分发动作。
- 优先传递明确的业务参数，避免为了菜单或弹层额外维护重复 UI 状态。
- 异步任务不得阻塞 GPUI 主线程；文件、网络和 SSH 操作放入现有异步执行链。
- 外部命令和系统关联启动必须检查错误，并将可理解的错误反馈到状态栏或日志。
- 不使用无意义的单字母变量，不添加与代码本身重复的注释。

## 国际化

- 所有用户可见文本必须使用 `rust_i18n::t!`。
- 新增键必须同步更新 `locales/zh-CN.yml` 和 `locales/en.yml`。
- 中文和英文菜单层级、启用状态与行为必须一致。

## SFTP 与 SSH

- 远程路径使用项目现有路径辅助函数，禁止按本地 Windows 路径规则处理。
- 删除、递归删除、覆盖上传、权限修改等危险操作必须保留明确确认和错误反馈。
- Windows 外部编辑器或系统关联仅在用户明确选择对应动作后启动；展开菜单或悬停不得启动任何进程。
- SSH Tab 的连接、断开、关闭、分离等操作必须保持 Tab 组与 Pane 状态一致。

## 验证要求

- 修改 Rust 代码后至少运行 `cargo check`。
- 涉及行为或业务逻辑时运行 `cargo test`。
- 提交前运行 `cargo clippy` 和 `git diff --check`。
- `cargo clippy` 中与本次修改无关的既有警告无需顺手修复，但必须在交付说明中指出。
- 不为通过格式化检查而批量格式化无关文件；只格式化本次触及的代码并检查最终 diff。
- UI 菜单修改需人工验证：右击打开、禁用项、子菜单、窗口边缘、点击外部关闭，以及 Windows 11 下无崩溃或异常进程启动。

## 变更交付

- 最终说明列出核心文件、行为变化和实际执行的验证命令。
- 未经用户明确要求，不创建分支、不提交、不推送。
