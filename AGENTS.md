# tiny-shell 项目协作规范

本文档适用于本仓库中的开发者与自动化编码代理。修改代码前应先了解相关模块、数据流和平台差异，并遵循以下约定。

## 1. 项目概况

- 项目名称：`tiny-shell`
- 技术栈：Rust 2024、GPUI、gpui-component、Tokio
- 最低 Rust 版本：`1.85.0`
- 支持平台：macOS、Windows、Linux
- 当前版本的唯一代码来源：`Cargo.toml` 中的 `[package].version`
- CI 质量门禁：格式检查、Clippy、测试和多平台 release 构建

## 2. 基本开发规范

### 2.1 修改原则

1. 先确认职责归属，再修改代码；不要把新逻辑继续堆入已有的大型 UI 或入口函数。
2. 优先复用已有模块和数据结构，避免复制实现、平行状态和重复配置。
3. 保持改动范围最小，不重构与当前任务无关的代码，不覆盖工作区中他人的未提交修改。
4. 优先使用清晰的数据流和纯函数；副作用集中在 UI 事件、I/O、网络和持久化边界。
5. 不使用 `unwrap()`、`expect()` 隐藏可恢复错误；错误应返回、记录或转化为用户可理解的状态。
6. 新增用户可见文本时，必须同时维护 `locales/zh-CN.yml` 和 `locales/en.yml`，不得在 UI 中散落硬编码文案。
7. 涉及文件路径、进程、终端、更新安装或窗口行为时，必须考虑 macOS、Windows、Linux 的差异。
8. 不提交密钥、令牌、真实服务器信息、用户配置、构建产物或调试日志。

### 2.2 模块职责

- `src/app/`：应用编排、窗口与 UI 状态、对话框、主题、启动流程和更新交互。
- `src/terminal/`：终端渲染、输入、选区和终端语义；不要混入 SSH 或持久化逻辑。
- `src/backend/`：本地终端与 SSH 后端实现，对上层提供稳定的运行接口。
- `src/session/`：会话配置、存储、SSH 配置和密钥管理。
- `src/sftp/`：SFTP 文件操作和文本文件处理。
- `src/sync/`：配置同步协议与实现。
- `src/system/`：本地及远程系统信息采集。
- `locales/`：所有用户可见文本的国际化资源。
- `scripts/`：各平台打包脚本；脚本参数和发布流水线必须保持一致。

跨模块功能应由 `src/app/` 负责协调，领域模块只暴露必要接口。若一次修改需要在多个方向回溯状态，优先调整模块边界或引入明确的状态对象，不通过持续增加布尔值和函数参数解决。

### 2.3 Rust 代码风格

- 遵循 `rustfmt`，不要手工维护与格式化工具冲突的排版。
- Clippy warning 视为错误，新代码不得引入 warning。
- 倾向使用不可变数据、迭代器、模式匹配和小型函数。
- 对领域概念使用有意义的类型，避免大量无语义的 `String`、元组和布尔参数。
- 公共接口应表达必要约束；仅供模块内部使用的实现保持私有。
- 注释解释设计原因、平台限制或安全约束，不复述代码表面行为。
- 异步任务必须明确生命周期、取消方式和状态回传路径，避免阻塞 UI 线程。

## 3. 验证规范

提交前至少执行：

```bash
cargo fmt --all -- --check
cargo clippy --locked --all-targets -- -D warnings
cargo test --locked --all-targets
```

涉及构建配置、依赖、平台代码或发布脚本时，还应执行适用平台的 release 构建：

```bash
cargo build --locked --release
```

若因平台或环境限制无法运行某项检查，应在变更说明中明确列出未验证项及原因，不得默认为已通过。

## 4. 提交与变更规范

- 每次提交只解决一个清晰问题，功能、重构和格式化尽量分开。
- 推荐提交信息格式：`<type>: <summary>`。
- 常用 `type`：`feat`、`fix`、`refactor`、`perf`、`docs`、`test`、`build`、`ci`、`chore`。
- 提交信息使用祈使式短句，说明变更目的，不使用“update files”之类无信息描述。
- 行为变化应同步更新相关文档；破坏性变化必须在发布说明中突出标记。

## 5. 版本号升级规范

本项目遵循 [Semantic Versioning 2.0.0](https://semver.org/)：`MAJOR.MINOR.PATCH`。

### 5.1 升级类型

- `PATCH`：向后兼容的缺陷修复、小型性能改进、文案或内部实现调整。例如 `1.0.5 -> 1.0.6`。
- `MINOR`：向后兼容的新功能、明显能力增强或新的可选配置。例如 `1.0.5 -> 1.1.0`。
- `MAJOR`：存在不兼容的配置格式、数据结构、命令行为或用户工作流变化。例如 `1.0.5 -> 2.0.0`。

仅修改文档、CI 或开发工具且不发布应用产物时，通常不单独升级版本。若这些修改需要随新安装包发布，则按实际用户影响选择版本级别。

### 5.2 版本来源与同步位置

1. `Cargo.toml` 的 `[package].version` 是应用版本的唯一代码来源。
2. 修改 `Cargo.toml` 后必须同步生成并提交 `Cargo.lock`，确保其中的 `tiny-shell` 包版本一致。
3. 应用界面和更新器通过 `env!("CARGO_PKG_VERSION")` 读取版本，不得新增硬编码版本常量。
4. `README.md` 与 `README.en.md` 只保留当前版本及最近版本的简要摘要，且中英文内容必须一致；完整更新内容统一维护在 `docs/upgrade/`。
5. `docs/upgrade/README.md` 是版本历史总览，必须维护当前版本、发布日期、详情链接和一句话摘要。
6. 每个发布版本必须建立独立详情文档 `docs/upgrade/vMAJOR.MINOR.PATCH/README.md`；目录名必须与 Git 标签完全一致，例如 `docs/upgrade/v1.0.6/README.md` 对应 `v1.0.6`。
7. 发布标签必须为 `vMAJOR.MINOR.PATCH`，并与 `Cargo.toml` 完全一致，例如 `v1.0.6`。
8. 打包脚本和 GitHub Actions 从 Git 标签读取产物版本，禁止发布标签与应用版本不一致的构建。

### 5.3 详细更新文档规范

版本升级不是只修改版本号。凡是准备发布应用产物的版本，必须在创建标签前完成对应的详细更新文档；缺少详情文档视为版本升级未完成。

详情文档必须基于实际代码差异、提交记录、配置变化和验证结果编写，不得仅复制提交标题，也不得记录尚未实现或未验证的能力。推荐按最近祖先发布标签到目标版本的差异整理；若版本来自并行发布线，应明确真实祖先关系，不能仅按版本号大小推断比较区间。

每份 `docs/upgrade/<version>/README.md` 至少包含以下内容：

1. **版本标题与元信息**：版本号、发布日期；预发布版本还应标明 `beta`、`rc` 等状态。未正式发布时不得虚构发布日期或声称标签已经存在。
2. **版本概述**：用一段面向用户的文字说明本次发布解决的问题、核心价值和主要影响。
3. **详细更新内容**：按实际内容拆分为新功能、改进、修复、性能、安全、工程或发布等分类；没有内容的分类不要保留空标题。
4. **行为与界面变化**：说明用户工作流、默认行为、设置项、快捷键、窗口交互、命令行为或可见文案的变化。
5. **配置与数据兼容性**：说明配置格式、存储路径、同步协议、加密数据、会话数据是否兼容，是否发生迁移以及能否回退。
6. **升级说明**：给出覆盖安装、备份、迁移、重启、权限或平台特定操作；无额外操作时也要明确写明可直接升级。
7. **破坏性变更与已知问题**：破坏性变化必须单独突出，包含影响范围、迁移方法和回退方案；存在已知限制时应写明触发条件及临时规避方式。
8. **验证结果**：列出本版本实际执行的格式检查、Clippy、测试、release 构建和平台验证；未执行项必须注明原因，不得写成已通过。
9. **变更依据**：提供当前标签、最近祖先版本和 GitHub compare 链接；首个版本应说明不存在前置比较区间。
10. **导航链接**：提供返回 `docs/upgrade/README.md` 的链接。

详细更新内容应描述“用户得到什么、行为如何变化、升级需要注意什么”，避免只写“优化代码”“修复若干问题”等无法核验的表述。涉及多个平台时，应分别说明 Windows、macOS、Linux 的差异；涉及安全问题时，不在发布文档中暴露密钥、真实服务地址或可直接滥用的敏感细节。

推荐使用以下结构，并根据版本实际内容删减可选分类：

```markdown
# TinyShell vX.Y.Z

> 发布日期：YYYY-MM-DD

## 版本概述

## 新功能

## 改进与修复

## 行为与界面变化

## 配置与数据兼容性

## 升级说明

## 破坏性变更与已知问题

## 验证结果

## 变更依据

[返回版本总览](../README.md)
```

### 5.4 版本升级步骤

1. 检查目标版本与最近发布标签之间的代码差异，确认本次变更属于 `PATCH`、`MINOR` 或 `MAJOR`。
2. 确认需要向用户说明的新功能、修复、行为变化、兼容性、迁移要求、平台差异和已知问题。
3. 修改 `Cargo.toml` 中的版本号。
4. 执行 `cargo check` 更新 `Cargo.lock`，确认其中的 `tiny-shell` 包版本一致，再执行 `cargo check --locked`。
5. 创建或更新 `docs/upgrade/<version>/README.md`，按 5.3 节要求填写完整详情和实际验证结果。
6. 更新 `docs/upgrade/README.md`，将新版本加入索引顶部，并同步当前版本、发布日期和一句话摘要。
7. 更新 `README.md` 与 `README.en.md` 的当前版本链接和最近版本摘要，确保中英文含义一致并链接到详情文档。
8. 完成格式、Clippy、测试和适用平台的 release 构建；将最终结果回填到版本详情文档。
9. 复核版本号、锁文件、文档目录、比较链接、构建产物命名和发布标签完全一致。
10. 单独提交版本升级和发布文档，不要混入未验证的新功能。
11. 在目标提交上创建与版本一致的标签并推送：

```bash
git tag v1.0.6
git push origin v1.0.6
```

标签推送后会触发 `.github/workflows/release.yml`，构建多平台产物并创建 GitHub Release。标签发布前必须再次确认版本号和详细更新文档，因为已公开发布的标签和版本不得复用；修复发布问题应升级到新的 `PATCH` 版本，并建立新的版本详情文档。

### 5.5 预发布版本

需要公开测试时可使用 SemVer 预发布版本，例如 `1.1.0-beta.1`，对应标签为 `v1.1.0-beta.1`。后续版本按 `beta.2`、`rc.1` 递增，正式版使用 `1.1.0`。每个对外发布的预发布版本同样必须建立独立详情文档并进入版本总览，不得覆盖同名标签或替代已发布的正式版本。
